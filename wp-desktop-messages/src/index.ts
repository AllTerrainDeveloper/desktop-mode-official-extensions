/**
 * Messages chat-window bundle entry. Loads the window render
 * callback registration. The shell module (`shell.ts`) is built
 * separately and ALWAYS-on; this bundle only loads when the native
 * window opens.
 *
 * @since 0.22.0
 */

import './window';
